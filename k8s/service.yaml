apiVersion: v1
kind: Service
metadata:
  name: churn-prediction-api
  namespace: mlops-platform
  labels:
    app: churn-prediction-api
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: churn-prediction-api
